import 'package:flutter/material.dart';

class TeamsChances extends StatelessWidget {
  const TeamsChances({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center();
  }
}
